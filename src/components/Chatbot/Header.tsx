import React from "react";

const Header: React.FC = () => (
  <header className="chatbot-header">
    <button className="back-btn">{'<'}</button>
    <span className="title">퍼퓨봇</span>
  </header>
);

export default Header;
